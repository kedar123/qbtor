<%= form_for(@testmeall) do |f| %>
  <% if @testmeall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testmeall.errors.count, "error") %> prohibited this testmeall from being saved:</h2>

      <ul>
      <% @testmeall.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
